<div class="block-center mt-xl wd-xl">
  <!-- START panel-->
  <div class="panel panel-dark panel-flat">
    <div class="panel-body">
      <p class="text-center pv">Formulário para partidas.</p>
      <%= form_for(@match) do |f| %>
  <% if @match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% @match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

          <%= flash[:notice] %>
  <div class="form-group has-feedback">
    <%= f.label :when, "Quando",:class => 'text-muted' %><br>
    <%= f.datetime_select :when %>
  </div>
  <div class="form-group has-feedback">
    <%= f.label :team_id, "Time 1",:class => 'text-muted' %><br>
    <%= f.select :team_id, @teams.map{ |u| [u.name, u.id] }, {}, :class => 'form-control', :style => "width:280px" %>
  </div>
  <div class="form-group has-feedback">
    <%= f.label :challenger_id, "Time 2",:class => 'text-muted' %><br>
    <%= f.select :challenger_id, @challengers.map{ |t| [t.name, t.id] }, {}, :class => 'form-control', :style => "width:280px" %>
  </div>
  <div class="form-group has-feedback">
    <%= f.label :where_id, "Campo",:class => 'text-muted' %><br>
    <%= f.text_field :where_id,:class => 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit'Enviar', :class => 'btn btn-block btn-primary mt-lg' %>
  </div>
<% end %>
    </div>
  </div>
  <!-- END panel-->
</div>

